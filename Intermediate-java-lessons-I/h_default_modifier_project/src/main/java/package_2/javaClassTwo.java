package package_2;

import package_1.atributes_methods;//we start importing the class w want to work with.

public class javaClassTwo {

    public static void main(String[] args) {
        atributes_methods mess = new atributes_methods();
        //mess. //uncomment "mess" and add a dot and see if the values are suggested.
    }
}
//We're in other package so here defaul won't allow us to use that variables
