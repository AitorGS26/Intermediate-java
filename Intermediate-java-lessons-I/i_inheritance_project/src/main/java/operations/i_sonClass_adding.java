package operations;

/*When we add the class name, we use extends and the father class name, for allowing to use its parts. */
public class i_sonClass_adding extends i_fatherClass {

    public void adding() {//the articles were declared at father class
        result = valueOne + valueTwo;
    }
    /*We create the method adding, whith void because we want to return a result */
}
