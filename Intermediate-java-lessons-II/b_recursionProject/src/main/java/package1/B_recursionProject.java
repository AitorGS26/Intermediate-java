package package1;

public class B_recursionProject {

    public static void main(String[] args) {
        /*Now in main class we add of course an object of recursion class called same way to comunicate both classes
        then we add the print mehod we've created and add the value that will be the first printing
         */
        recursion recursion = new recursion();
        recursion.print(1);
    }
}
